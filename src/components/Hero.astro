<div class="hero">
    <div class="hero-content">
      <h1 class="hero-title skewed"> WESHELTER </h1>
      <h1 class="hero-title overlap">WESHELTER</h1>
    </div>
  </div>

  
  <style>

.hero {
    position: relative;
    width: 100%;
    height: 60vh; 
    background-image: url('/HeroImageWeshelter.jpg');
    background-size: cover;
    background-position: center;
    display: flex;
    align-items: center;
    justify-content: center; 
}

.hero-content {
    position: relative;
    text-align: center;
    transform: translateY(200px); 
    left: auto;
    width: 100%; 
}

.hero-title {
    font-size: clamp(40px, 10vw, 70px);
    font-weight: 720;
    position: relative; 
    font-family: var(--font);
    padding-bottom: 0.2em;

}

.skewed {
    color: blue;
    transform: translateY(200%) translateX(-70px) rotate(180deg);
    z-index: 2;
    
}

.overlap {
    color: rgb(0, 0, 0); 
    z-index: 3;
}

@media (min-width: 995px) and (max-width: 1199px) {
    .hero {
        background-size: 80%;
        background-position: right center;
        background-repeat: no-repeat;
    }

    .hero-content {
        transform: translateY(50px) translateX(0); 
    }

    .hero-title {
        font-size: clamp(50px, 8vw, 80px); 

    }

    .overlap {
        transform: translate(-350px, 10px) rotate(90deg); 
    }

    .skewed {
        transform: translate(-450px, 20px) rotate(270deg); 
    }
}

@media (min-width: 1200px) {
    .hero {
        background-size: 80%;
        background-position: right center;
        background-repeat: no-repeat;
    }

    .hero-content {
        transform: translateY(50px) translateX(0); 
    }

    .hero-title {
        font-size: 96px; 
    }

    .overlap {
        transform: translate(-500px, 10px) rotate(90deg); 
    }

    .skewed {
        transform: translate(-600px, 20px) rotate(270deg); 
    }
}



  </style>