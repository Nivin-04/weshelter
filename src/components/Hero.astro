<div class="hero">
    <div class="hero-content">
      <h1 class="hero-title skewed"> WESHELTER </h1>
      <h1 class="hero-title overlap">WESHELTER</h1>
    </div>
  </div>

  
  <style>

.hero {
    position: relative;
    width: 100%;
    height: 60vh; 
    background-image: url('/HeroImageWeshelter.jpg');
    background-size: cover;
    background-position: center;
    display: flex;
    align-items: center;
    justify-content: center;
    
}

.hero-content {
    position: relative;
    text-align: center;
    transform: translateY(150px) translateX(-150px);
}

.hero-title {
    font-size: 10vw;
    font-weight: 720;
    position: absolute;
    font-family: var(--font);
}
.skewed {
    color: blue;
    transform: translateY(60px) translateX(-50px) rotate(180deg);
    z-index: 2;
}

.overlap {
    color: rgb(0, 0, 0); 
    mix-blend-mode: difference;
    z-index: 3;
}


@media (min-width: 700px) {
    .hero {
        background-size: 80%;
        background-position: right center;
        background-repeat: no-repeat;
        margin-right: 0;
    }

    .hero-content {
        transform: none; /* Reset previous transforms */
        rotate: 90deg; /* Alternative syntax */
    }

    .hero-title{
        font-size: 8vw;
    }

    .overlap {
        transform: none; /* Reset */
        translate: -100px 220px ; /* Ensures new transform applies */
    }
    .skewed {
        transform: none; /* Reset */
        translate: -200px 320px;
        rotate: 180deg; /* Ensures new transform applies */
    }
}



@media (min-width: 1000px) {
   

    .hero-content {
        transform: none; /* Reset previous transforms */
        rotate: 90deg; /* Alternative syntax */
    }

    .hero-title{
        font-size: 8vw;
        position: fixed;
    }

}


  </style>