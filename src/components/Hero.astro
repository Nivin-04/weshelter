<div class="hero">
    <div class="hero-content">
      <h1 class="hero-title skewed"> WESHELTER </h1>
      <h1 class="hero-title overlap">WESHELTER</h1>
      <h1 class="hero-title black">WESHELTER</h1>
    </div>
  </div>
  
  
  <style>



.hero {
    position: relative;
    width: 100%;
    height: 60vh; 
    background-image: url('/HeroImageWeshelter.png');
    background-size: cover;
    background-position: center;
    display: flex;
    align-items: center;
    justify-content: center;
}

.hero-content {
    position: relative;
    text-align: center;
    transform: translateY(150px) translateX(-150px);
}

.hero-title {
    font-size: 10cqw;
    font-weight: 720;
    position: absolute;
    font-family: var(--font);
}

.black {
    font-size: 10cqw;
    font-weight: 720;
    position: absolute;
    /* transform: translateY(10px); */
    font-family: var(--font);
    color: black;
    z-index: 1; /* Ensures it stays visible */
}

/* Skewed blue text */
.skewed {
    color: blue;
    transform: translateY(60px) translateX(-50px) rotate(180deg);
    z-index: 2;
}

/* Overlap text that blends with the image */
.overlap {
    font-size: 10cqw;
    font-weight: 720;
    position: absolute;
    font-family: var(--font);
    color: white; /* Makes it visible */
    background: linear-gradient(to bottom, white 50%, black 50%);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    mix-blend-mode: difference;
    z-index: 2;
}

  </style>