<form class="menu">
<input type="checkbox" id="checkbox" />
<label for="checkbox" class="burger-menu">☰</label>
<nav>
        <ul>
            <li><a href="">Få Hjælp</a></li>
            <li><a href="">Bliv Frivillig</a></li>
            <li><a href="">Om Weshelter</a></li>
            <li><a href="">Kontakt Os</a></li>
        </ul>
    </nav>
</form>

<style>

#checkbox{
        display: none;
    }

.burger-menu{
        cursor: pointer;
        display: inline-block;
        position: relative;

    }
nav ul {
        display: none;
        position: absolute;
        width: calc(100% - 60px);
        transform: translateX(-100%);
        top: 10px; 
        background-color: white;
        padding: 10px;
    }
ul li{
        list-style: none;
    }
ul li a{
        text-decoration: none;
        color: black;
        background-color: white;
        text-align: center;
        padding: 3px 0 4px 5px; 
        line-height: 2.5em;
        width: 100%;
        display: inline-block;
        font-family: var(--font);
      }


nav ul li a:hover {
  background-color: var(--blue);
  color: var(--white);
}

#checkbox:checked + label + nav ul {
    display: block;
    z-index: 3;
  }


  @media (min-width: 995px) {
    .burger-menu{
        display: none;
    }

    nav{
        position: relative;
        left: -40em;
    }
   nav ul{
    display: flex;
    position: relative;
    gap: 4em;
    left: 60em;
    
   }

   nav ul li{
    display: inline;
    text-wrap: nowrap;
    display: flex;
   
   }

   nav ul li a:hover {
  background-color: var(--white);
  color: var(--blue);
}
}


</style>